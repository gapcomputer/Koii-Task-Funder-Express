{
  "name": "hero-api",
  "version": "1.0.0",
  "description": "Hero API Endpoint",
  "scripts": {
    "test": "vitest"
  },
  "devDependencies": {
    "@types/express": "latest",
    "supertest": "latest",
    "vitest": "latest"
  },
  "dependencies": {
    "express": "latest"
  }
}